data_params:
  data_dir: data/
  data_classes: ['wmv', 'wrl']
  nature_split: [60 , 20, 20]
  num_workers: 0
  patch_size: 148
  batch_size: 64
  raw_suffix: raw
  swap_suffix: swap
exp_params:
  LR: 0.00001
  manual_seed: 1265
  weight_decay: 0.0
logging_params:
  name: VQVAE
  save_dir: logs/
model_params:
  beta: 0.25
  embedding_dim: 64
  img_size: 148
  in_channels: 3
  name: resnet18
  num_embeddings: 512
trainer_params:
  device: "cuda:0"
  gpus:
  - 1
  num_epochs: 25
  patience: 10
